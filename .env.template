
# ==============================================
# ENVIRONMENT VARIABLES TEMPLATE
# ==============================================
# Copy this file and set actual values in Replit Secrets tab
# DO NOT commit actual secret values to Git!

# ----- Flask Application Secrets -----
# Used for Flask session management
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# Used for JWT token signing
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=

# ----- Email Configuration -----
# SMTP email server (default: smtp.gmail.com)
MAIL_SERVER=smtp.gmail.com

# SMTP port (default: 587 for TLS)
MAIL_PORT=587

# Enable TLS (default: True)
MAIL_USE_TLS=True

# Your email address
MAIL_USERNAME=

# Your email password or app-specific password
# For Gmail: Create an app password at https://myaccount.google.com/apppasswords
MAIL_PASSWORD=

# Email sender address (optional, defaults to MAIL_USERNAME)
MAIL_DEFAULT_SENDER=

# ----- Database Configuration -----
# PostgreSQL connection string (automatically set by Replit)
# Format: postgresql://user:password@host:port/database
DATABASE_URL=

# ----- Application Configuration -----
# Environment mode (development or production)
FLASK_ENV=development

# CORS origins (default: * for all origins)
CORS_ORIGINS=*

# ----- Frontend Configuration (Vite) -----
# API endpoint URL
VITE_API_URL=/api

# ==============================================
# SETUP INSTRUCTIONS
# ==============================================
# 1. Go to Replit Tools → Secrets (or click the lock icon 🔒)
# 2. Add each variable listed above with its actual value
# 3. Required secrets (must be set):
#    - SECRET_KEY
#    - JWT_SECRET_KEY
#    - MAIL_USERNAME
#    - MAIL_PASSWORD
# 4. Optional secrets (have defaults):
#    - MAIL_SERVER (defaults to smtp.gmail.com)
#    - MAIL_PORT (defaults to 587)
#    - MAIL_USE_TLS (defaults to True)
#    - DATABASE_URL (auto-set by Replit PostgreSQL)
# 5. Restart your application after adding secrets

# ==============================================
# SECURITY NOTES
# ==============================================
# - Never commit actual secret values to Git
# - Use strong, randomly generated keys for SECRET_KEY and JWT_SECRET_KEY
# - For Gmail: Use app-specific passwords, not your actual password
# - When transferring Repl: manually re-add all secrets in the new account
# - Secrets persist across deployments automatically
